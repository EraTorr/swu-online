
export const executeStep = (data: any) => {
    const step = data.step;

    console.log(step, data)

    switch (step) {
        case 'initGame':
            
            break;
        case '':
            break;
    }
}